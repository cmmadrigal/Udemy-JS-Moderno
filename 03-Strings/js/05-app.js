const producto = "Monitor 20 pulgadas";

const texto = ' en promocion'.repeat(3);

console.log(`${producto} ${texto} `);

const tweet = "aprendiendo js moderno #js";
console.log(tweet.split("#"));
console.log(producto.split('"'));
console.log(producto.split(" "));
